include required("projects/ThriftExample/ideprobe.conf")

query = "ThriftStruct"
navigationTargets = [
  { name = "ThriftStruct", location = "(foo.bar)" }, # generated java file
  { name = "ThriftStruct", location = "struct.thrift" } # original thrift source
]
